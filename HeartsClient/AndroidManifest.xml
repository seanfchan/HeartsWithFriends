<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="org.bitcoma.heartsClient"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".SplashActivity"
                  android:label="@string/app_name"
                  android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    <activity android:name="AppHomeActivity" android:label="AppHome" android:theme="@android:style/Theme.NoTitleBar"></activity>
<activity android:name="AuthActivity" android:label="Authentication" android:theme="@android:style/Theme.NoTitleBar"></activity>
<activity android:name="InstructionsActivity" android:label="Instructions" android:theme="@android:style/Theme.NoTitleBar"></activity>
<activity android:name="MultiHomeActivity" android:label="MultiPlayer" android:theme="@android:style/Theme.NoTitleBar"></activity>
</application>
    <uses-sdk android:minSdkVersion="7" />

</manifest> 